# 第一次培训作业报告

## 1. 开发环境、坐标系约定和重投影原理

### 1.1 开发环境
- 操作系统：Windows 10/11 + WSL2 (Ubuntu)
- 代码编辑器：VS Code
- 构建工具：CMake
- 编程语言：C++17
- 第三方库：Eigen3 (用于矩阵运算)

### 1.2 坐标系约定
- 世界坐标系到相机坐标系的变换：`Pc = R * Pw + t`
- 其中 `R` 为 3x3 旋转矩阵，`t` 为 3x1 平移向量，三维坐标与平移量使用相同长度单位（如米）。

### 1.3 理想针孔模型重投影原理
- 忽略畸变，相机内参为 `fx, fy, cx, cy`。
- 相机坐标系下的点 `Pc = (Xc, Yc, Zc)` 投影到像素坐标 `(u, v)` 的公式为：
  `u = fx * Xc / Zc + cx`
  `v = fy * Yc / Zc + cy`
- **异常处理约定**：当 `Zc <= 0`（点位于相机后方或中心）时，抛出 `std::runtime_error` 异常，避免无效投影。

---

## 2. 项目结构、构建运行方法、测试结果及误差分析

### 2.1 项目结构（分离头文件与实现文件）
- `include/reprojection.h`：声明数据结构（如 `CameraIntrinsics`）和函数接口（如 `projectPoint`）。
- `src/reprojection.cpp`：实现核心矩阵运算和投影逻辑，包含异常处理。
- `src/main.cpp`：构造测试数据，调用函数并计算与观测点的像素欧氏距离。
- `CMakeLists.txt`：构建脚本，配置 C++17 标准并链接 Eigen 库。

### 2.2 构建运行方法
在终端（WSL Ubuntu）中执行：
```bash
mkdir build && cd build
cmake ..
make
./reprojection_test
### 2.3 测试结果
程序编译并运行后，输出结果如下：
- 正常投影测试：
  - 计算得到的像素坐标：(680.00, 400.00)
  - 观测到的像素坐标：(682.00, 398.00)
  - 像素欧氏距离误差：2.83 pixels
- 非正深度异常测试：
  - 成功捕获异常：“错误：该点位于相机后方或中心，无法投影 (Zc <= 0)”

### 2.4 误差分析
由于构造测试数据时故意引入了微小偏差（观测点与理论计算点存在像素差），计算得到的欧氏距离反映了重投影误差。该误差在合理范围内，验证了投影公式的正确性。

---

## 3. 遇到的问题与解决办法
- **问题1：WSL2 安装报错 0x80071772。**
  - 解决：将 Windows 的“新应用保存位置”改回 C 盘，重新安装即可。
- **问题2：`make` 命令卡死无响应。**
  - 解决：因 WSL2 的 localhost 代理配置冲突导致。通过关闭系统代理，并重启 WSL 解决。
- **问题3：编译报错 `Eigen/Dense: No such file or directory`。**
  - 解决：运行 `sudo apt install libeigen3-dev` 安装库，并在 `CMakeLists.txt` 中正确添加 `find_package(Eigen3 REQUIRED)` 和 `target_link_libraries`。
- **问题4：误将 `build/` 文件夹提交到 Git 仓库。**
  - 解决：创建 `.gitignore` 文件并写入 `build/`，使用 `git rm -r --cached build` 将其从 Git 追踪中移除。

---

## 4. 代码仓库链接、PR链接和最终提交的commit hash
- **代码仓库链接**：https://github.com/robomaster-wyx/RM
- **PR链接**：https://github.com/robomaster-wyx/RM/pull/2
- **最终提交的 commit hash**：f617e5a